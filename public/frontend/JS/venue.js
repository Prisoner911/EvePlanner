// JavaScript to trigger hover effect after a delay
setTimeout(function() {
    var animatedText = document.getElementById('animatedText');
    animatedText.classList.add('hovered');
}, 2000); // Adjust the delay time (in milliseconds) as needed
